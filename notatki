

int * tab[N]= new int[N];

getline może wczytywać linie tekstu do danego znaku 
getline(cin, napis); / getline(cin, tekst, 'x');

zamiana duzych literek na male 
if (c >= 'A' && c <= 'Z')
            c = c + 32; 

Tablica dynamiczna!!!
int** tab = new int *[w]; //przydzielenie pamięci na w wierszy
	
for(int i=0;i<w;i++)
            tab[i] = new int[k]; //przydzielenie dla każdego wiersza po k komórek
delete[] tab; //zwalnianie pamieci

setw() - ustawianie odstepu miedzy liczbami 


WCZYYWANIE PLIKÓW
ifstream plik("lokomotywa.txt");
    if (!plik.is_open()) {
        cout << "Nie udalo sie otworzyc pliku!" << endl;
        return 1;
    }
    
    string linia, wyraz;
    string caly_tekst;
    
    plik>>wyraz;
    cout<<wyraz;
    getline(plik, linia);
    cout<<linia;
    
    
      
    
    plik.close();



rzutowanie na int
static_cast<int>(a)

floor(a) ---zwraca najwieksza liczbe calkowita mniejsza lub rowna a



strona konwersji do U1
https://eduinf.waw.pl/inf/alg/006_bin/0017.php

